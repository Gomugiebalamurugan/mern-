// export const add = (a, b) => a + b;
// export const mul = (a, b) => a * b;             
// export const sub = (a, b) => a - b;
// export const div = (a, b) => a / b;

const mathOperations = {
    add: (a, b) => a + b,
    mul: (a, b) => a * b,
    sub: (a, b) => a - b,
    div: (a, b) => a / b
}               

export default mathOperations;
console.log(math.div(540, 3))
